export interface ExtractedCategoryInfo {
     categoryName: string | null;
     typeName: string | null;
}

export interface ExtractedUpdateInfo {
     oldName: string | null;
     newName: string | null;
}
